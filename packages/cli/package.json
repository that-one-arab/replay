{
  "name": "@rec/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "files": ["dist"],
  "bin": { "rec": "./dist/main.js" },
  "dependencies": {
    "@rec/core": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "node --test dist/**/*.test.js"
  }
}
