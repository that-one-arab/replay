{
  "name": "@rec/mcp",
  "version": "0.2.2",
  "private": true,
  "type": "module",
  "files": [
    "dist"
  ],
  "bin": {
    "rec-mcp": "./dist/main.js"
  },
  "dependencies": {
    "@playwright/mcp": "0.0.78",
    "@rec/core": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "node --test dist/main.test.js"
  }
}
