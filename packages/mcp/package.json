{
  "name": "@replay/mcp",
  "version": "0.2.2",
  "private": true,
  "type": "module",
  "files": [
    "dist"
  ],
  "bin": {
    "replay-mcp": "./dist/main.js"
  },
  "dependencies": {
    "@playwright/mcp": "0.0.78",
    "@replay/core": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "node --test dist/main.test.js"
  }
}
